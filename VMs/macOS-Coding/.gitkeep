# Placeholder - Git keeps this folder
# Actual VM disk images are too large for Git (>60GB)
# Create with: qemu-img create -f qcow2 macos-coding.qcow2 60G
# Note: macOS VM requires additional CPU patches and OpenCore bootloader
